import { Container } from "./styles";

export function InfoMovie({ info }) {
    return (
        <Container>
            <header>
                <img src={info.logo_serie} alt="series logo" />
            </header>
            <main>

            </main>
        </Container>
    )
}